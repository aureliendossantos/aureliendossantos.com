---
interface Props {
	bg?: string
	text?: string
}
const { bg, text } = Astro.props
---

<div style={{ backgroundColor: bg || "#808080", color: text || "#f0f0f0" }}>
	<div class="n medium:pl-8 m-auto max-w-[1100px] py-16 pr-8 pl-80 text-xs">
		<slot />
	</div>
</div>

<style is:global>
	@reference "#tailwind.css";

	.n {
		font-family: Louize, Arial, sans-serif;
	}
	p {
		@apply mb-[1em];
	}
	img {
		@apply inline shadow-xs;
	}
	.gallery {
		figure + figure {
			@apply !ml-6;
		}
		figure:has(img):first-of-type {
			@apply medium:-ml-8;
		}
		figure:has(img):last-of-type {
			@apply medium:-mr-8;
		}
		figcaption {
			@apply medium:mr-8;
		}
	}
	figcaption {
		@apply text-right;
	}
</style>
