---
import crypto from "node:crypto"
const uid = crypto.randomUUID()
---

<label for={`sn-${uid}`} class="margin-toggle sidenote-number"></label>
<input type="checkbox" id={`sn-${uid}`} class="margin-toggle" />
<span class="sidenote"><slot /></span>
