---
import Sidenote from "./Sidenote.astro"

interface Props {
	paragraph?: boolean
	hide?: boolean
	script?: boolean
	noMarginTop?: boolean
	mobileHalfWidth?: boolean
}
const { paragraph, hide, script, noMarginTop, mobileHalfWidth } = Astro.props
---

{
	paragraph ? (
		<p>
			<Sidenote label={hide ? "plus" : "none"} toggle={hide} visible={!hide} noMarginTop={noMarginTop} mobileHalfWidth={mobileHalfWidth} script={script}>
				<slot />
			</Sidenote>
		</p>
	) : (
		<Sidenote label={hide ? "plus" : "none"} toggle={hide} visible={!hide} noMarginTop={noMarginTop} mobileHalfWidth={mobileHalfWidth} script={script}>
			<slot />
		</Sidenote>
	)
}
